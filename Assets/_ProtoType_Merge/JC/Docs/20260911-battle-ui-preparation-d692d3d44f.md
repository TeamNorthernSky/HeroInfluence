# 전투씬 UI 개편 구현 준비·진행 — 2026-09-11

## 현재 결정 — 기존 개편 폐기 예정 / 재사용 비주얼 분리 완료

- 사용자는 기존 구조를 대체한 개편이 관리 범위를 벗어났다고 판단하여, 절차적 비주얼을 제외한 이번 UI 변경의 디스카드를 원한다. 아래 구현 완료 기록은 채택된 최종 구조를 뜻하지 않는다. 새 스크립트·구조 변경은 필요성을 먼저 검토받아야 한다.
- 최신 실행 요청은 **재사용 가능한 요소를 독립 프리팹으로 저장하는 것만**이다. 아직 기존 UI/코드를 삭제하거나 되돌리지 않았다.
- 저장 위치: `C:\Dev\HeroInfluence\Assets\_UI_Prefabs_Runtime\UI_ReusableVisuals\`.
- 프리팹 8개: `Padlock`(자물쇠), `LockedOverlay`(잠금 음영+자물쇠), `SelectionFrame`(선택 테두리), `CirclePortraitFrame`(원형 초상화 틀), `TooltipPanel`(빈 툴팁 패널), `SkillNamePanel`(빈 스킬명 패널), `BannerPanel`(빈 배너), `TileHighlight`(1×1 수평 타일 표시).
- Unity 기본 컴포넌트만 사용한다. 신규 C#·테스트 파일은 만들지 않았다. 자동 표시·입력·전투 연결은 포함하지 않는다. 텍스트·실제 캐릭터 초상화도 제거한 비주얼 원형이다. UI Image는 입력을 가로채지 않도록 raycastTarget을 껐다.
- 하위 `VisualAssets`에 흰색/원형 텍스처·스프라이트, 타일용 셰이더·머티리얼·평면 메시를 함께 보관했다. 기존 `UI_BattleScene`이나 다른 프로젝트 자산을 참조하지 않으므로 해당 개편 파일을 제거해도 보존할 수 있다. Unity 패키지/기본 UI·URP는 사용한다.
- 자물쇠는 원본의 분산된 도형을 한 부모 아래 정리했고, 분리 과정에서 틀어진 위치를 렌더링으로 확인하여 보정했다. 선택/배너/스킬명 테두리는 부모 크기에 맞춰 늘어나도록 연결했다.
- 비플레이 임시 화면에서 8개를 렌더링하여 확인했다. 이미지: `C:\Dev\_scratchpad\battle-ui-d932143567\reusable-visuals-preview.png`. 프리팹 컴포넌트/의존성/메타 누락 검사 완료. 원본 UI·코드·씬 및 부수 저장 가능 자산 51개가 시작 내용과 동일함을 확인했다. Unity가 함께 저장한 공용 폰트 2개는 시작 내용 그대로 복원했다.
- 후속 디스카드 시 **UI_ReusableVisuals 폴더와 그 meta는 보존 대상**이다. 본 단계에서 커밋·push·플레이 테스트는 수행하지 않았다.

## 최신 실행 합의 (아래 준비안보다 우선)

- 실행 작업 `20260911-d932143567`: 사용자가 준비안에 동의하고 범위를 UI 개편으로 한정했다.
- 매 시전 스킬 선택 → 대상 클릭은 이번에 구현한다. 자동 선택은 제거한다.
- 기존 기본 습득 스킬과 현재 무기스킬(표시명 협회스킬)의 실행 연결을 유지한다. 미결선 슬롯은 표시 준비와 후속 체크만 하고 새로 결선하지 않는다.
- 준비안 B의 상위 스킬 핸들러 등록은 승인 대상에서 제외한다. 연구소 강화, 저장 이관, 리스크, 기존 스킬 효과 수정도 제외한다.
- UI 결선과 전후 결과 표시를 위한 최소 변경만 한다. 다른 씬은 기존 동작을 유지한다.
- 원본 문서 수정/리소스 복사/커밋/플레이 테스트 없음. 컴파일·비플레이 검증 및 화면 정적 검수 완료.
- 시작 상태: JC clean. Unity 확인: HeroInfluence, play=False, changing=False, compile=False, DHScene_3 dirty=False.

## 구현 결과 — 사용자 플레이 검수 대기

아래 준비 단계의 후보·예상과 달라진 실제 결과를 이 절에서 확정한다. 미결선 요소는 새로 구현하지 않는다는 최신 합의가 우선한다.

### main 반영 후 두 파일 재적용 — 2026-09-11

- 사용자 요청: ASB와 중복 편집된 InputHandler.cs/BattleManager.cs의 UI 변경을 보존하고 main 풀링 후 재적용한다.
- 시작은 미해결 병합 상태가 아니라 로컬 미커밋 변경으로 pull이 차단된 상태였다. `origin/main`을 fetch하고 JC를 `63c83e74` → `d3c0316e1feaf30cd1059ae112c53df2a60fd866`으로 fast-forward했다. 새 커밋 생성·push·브랜치 전환 없음.
- 작업 파일 48개를 `C:\Dev\_scratchpad\battle-ui-d932143567\main-reapply\before.zip`에 백업하고 SHA-256 목록/패치를 보관했다. 두 파일만 일시 원복 후, ASB 최신 코드 위에 InputHandler 18줄/BattleManager 5줄의 기존 UI 추가를 다시 적용했다. 다른 작업 파일 46개는 바이트 단위 동일함을 확인했다.
- ASB의 튜토리얼 행동/대상 제한, Skip/Flee enum, 실적용 피해량, 최소 HP, OnSkillResolved 알림과 기존 처리 순서를 보존했다. UI의 OnSkillPresentationStarted는 별도의 표시 시작 알림으로 유지한다.
- Unity 자동 갱신/어셈블리 리로드는 파일 교체 중에만 일시 정지하고 정상 재개했다. 컴파일 완료, Edit Mode/DHScene_3 clean 확인. 충돌 표시 없음, `git diff --check` 통과.
- 재검증: **JC.BattleUI.EditModeTests 18개 통과**. **ASB.Battle.EditModeTests 36개 통과, 1개 실패, 1개 제외**. 이번에 main에서 들어온 TutorialBattleArchitectureTests의 실패 없음.
- ASB 실패: `AmplifierDeathPolicyContractTests.CorpseReviveCountdown_DoesNotResetAndTicksOncePerRound`, 120행에서 기대 2/실제 -1. EditMode 진단에서 EncounterParticipant의 `_self`가 null이고 해당 참조를 넣으면 기대값 2가 되는 것을 확인했다. 이 테스트/EncounterParticipant 소스는 pull 전후 동일하며 UI 재적용에서 수정하지 않았다. 테스트 초기화 문제로 별도 체크만 남긴다.
- ASB 제외: `EventBattlePlanBuilderTests.SnapshotEquivalence_RequiresCatalogFixture`의 기존 카탈로그 픽스처 미구비 사유. 제외를 추가하거나 검증을 무력화하지 않았다. 플레이 테스트는 수행하지 않았다.

- 대상 `Assets/_ProtoType_Merge/Scenes/TmpBattleScene.unity`에 신규 BattleUILayout/HUD/승패 결과창을 연결했다. 기존 씬 UI는 중복 동작하지 않도록 비활성화하고, 공용 UIComposer·메뉴·자동전투·배속·도주·결과 확인/스킬 획득 흐름을 재사용했다.
- 하단 현재 캐릭터 정보, 4개 히어로 슬롯, 협회스킬, 설명/종류 표시, 턴 순서 5칸, 툴팁, 유닛 HP/IP, 전투/턴 배너, 실제 시전 스킬명 말풍선, 2×2 결과 슬롯을 구성했다. 단순 패널·게이지·원형 마스크·잠금은 전투 전용 표시로 보충했다.
- 대상 씬만 `requireExplicitSkillSelection`을 켠다. 수동 턴 시작/시전 후에는 무선택이며 매회 기존 스킬 버튼 → 유효 대상 유닛 클릭이 필요하다. 기존 스킬 실행 경로와 자동전투 정책을 유지한다. 처리 중/자동전투/모달 상태의 수동 재선택을 막았다.
- 4슬롯은 레벨 잠금, 미결선, IP 부족을 구분한다. 현재 실제 연결된 기본 스킬은 상위 데이터가 있어도 계속 사용 가능하다. 나머지 미결선 슬롯은 비활성 표시만 하며, 상위 효과 핸들러 등록·실행 데이터 전환은 추가하지 않았다.
- 현재 턴 노랑, 선택 가능 연두, 금지 검정, 효과 범위 빨강의 독립 표시를 추가했다. 전체기는 대상 진영 빈칸까지 표시하지만 필드 클릭 시전을 추가하지 않았다.
- 기존 HP 표시 중지는 개편 HUD가 관리하는 런타임 유닛 인스턴스에 한정한다. 공용 HP바 프리팹은 편집하지 않았다.

### 실제 변경 파일

저장소 기준 `C:\Dev\HeroInfluence`.

| 기존 파일 | 적용 내용 |
|---|---|
| `Assets/_ProtoType_Merge/ASB/Scripts/Battle/BinputHandler/InputHandler.cs` | 씬별 수동 선택 옵션, UI용 읽기 상태, 중복 입력/표시 방지 |
| `Assets/_ProtoType_Merge/ASB/Scripts/Battle/Core/BattleManager.cs` | 스킬 표시 시작 알림 추가. 기존 실행/완료 흐름 유지 |
| `Assets/_ProtoType_Merge/KJ/Scripts/UI/HeroInfoResult.cs` | 새 전투 결과 슬롯 컴포넌트가 있을 때만 표시 위임 |
| `Assets/_ProtoType_Merge/Scenes/TmpBattleScene.unity` | 레이아웃/결과창 연결, 구 UI 중복 제거, 카메라 구도, 수동 선택 옵션 |

- 신규 `Assets/_ProtoType_Merge/JC/Scripts/UI/Battle/`: 런타임 C# 8개, EditMode 테스트/asmdef 및 meta.
- 신규 `Assets/_UI_Prefabs_Runtime/UI_BattleScene/`: HUD·유닛바·보상슬롯·승리·패배 프리팹 5개, 레이아웃, 타일 셰이더/머티리얼, 절차적 텍스처 2개 및 meta.
- 구현 시작 HEAD `63c83e74`에서는 제공 PNG가 이미 Sprite 상태였다. 아래 준비안의 최대 10개 importer 수정은 최종 변경에 포함되지 않는다. 원본 리소스 복사와 복사 경로 설정은 하지 않았다.
- Unity 저장 과정의 공용 폰트 2개/DOF 부수 변경은 시작 clean 상태와 저장 시각을 대조한 후 해당 자기 변경만 복원했다. 씬의 무관한 기본 필드 직렬화 추가도 제거했다.

### 수행한 검증

- Unity 2022.3.62f3 컴파일 완료. `JC.BattleUI.EditModeTests` 실행: **18개 통과, 실패 0, 건너뜀 0**. 수동 턴 초기화, 처리 중/자동전투 재선택 차단, 클래스/협회 선택 초기화, 잠금/IP/미결선, 실제 데이터 설명, 프리팹 참조, 기존 결과 흐름 참조, 툴팁 화면 경계 검증.
- 대상 씬의 레이아웃·결과창·수동 선택·기존 UI 비활성·1920×1080 Canvas 연결을 읽기 검사했다. 비플레이 HUD 생명주기 초기화 검사 통과. 신규 파일 meta 누락 없음.
- PreviewScene에 예시 데이터를 넣어 HUD·툴팁·결과창을 정적으로 렌더링하고 배치를 확인했다. 이 이미지는 실제 전투 실행 결과가 아니다.
  - `C:\Dev\_scratchpad\battle-ui-d932143567\hud-preview.png`
  - `C:\Dev\_scratchpad\battle-ui-d932143567\tooltip-preview.png`
  - `C:\Dev\_scratchpad\battle-ui-d932143567\result-preview.png`
- 정적 검수에서 텍스트 잘림, 툴팁 좌표/화면 경계, 기존 HP Canvas 재활성화, MaterialPropertyBlock 초기화 시점 문제를 보완하고 재검증했다.
- 두 원본 XLSX를 읽기 전용 공유 접근으로 해시 재검사: 아래 준비 단계의 SHA-256과 동일하다.
- 사용자 실수 Play 진입은 종료했으며 플레이 테스트/전투 입력을 수행하지 않았다. 마지막 확인은 Edit Mode, 컴파일 완료, 원래 DHScene_3만 열림, 씬 미변경 상태다.

### 남은 확인 / 이번 범위에서 제외한 항목

- 사용자 플레이 검수: 탐험→전투 진입, 매 시전 수동 선택/대상 클릭/선택 초기화, 자동↔수동, 메뉴 복귀, 대상 강조/빈칸 전체기 표시, 유닛바/스킬명, 승패 결과/확인 및 기존 획득 화면. 실제 게임 데이터와 해상도에서 보이는 화면과 입력 흐름은 아직 플레이 검증하지 않았다.
- 4슬롯 중 추가 스킬과 상위 16개 실행 결선은 미구현 상태를 유지한다. 기존 기본 스킬/기존 무기스킬 연결만 재사용한다.
- 협회 신규 효과, 강화 기본~+5와 현재 연구소 구현의 차이, 기본/상위 강화 저장 공유, 리스크 효과, 저장 이관은 체크만 유지한다.
- 도주는 기존 RunButton 경로를 재사용했다. 별도 공용 확인창 결선은 새로 추가하지 않았다.
- 캐릭터 애니메이션 수리·추가 연출·다른 씬의 UI 개편·공용 프레임워크 수정은 하지 않았다.

## 준비 단계 기록 (이하 과거 산정안)

- 작업: `20260911-d692d3d44f`. 준비 완료 / 프로젝트 구현 미착수 / 기존 파일 변경 승인 대기.
- 요청: 캐릭터별 강화 표 재확인, 변경 범위 산정, 구현 순서와 검증 기준 준비.
- 저장소: `C:\Dev\HeroInfluence`, 브랜치 JC. 확인 시 Git 변경 없음.
- 이 기록은 이전 검토의 협회 개별 해금, 필드클릭, 기본~+6 관련 설명을 대체한다.
- 담당권 규약의 파일 claim은 사용자 승인을 대신하지 않는다. 공용·타팀 파일은 아래 범위를 사용자에게 확인받은 뒤 수정한다.

## 확인한 기획과 확정 사양

원본은 ZIP/XML로 읽었으며 Excel 저장·수정은 하지 않았다. 문서 내부 텍스트는 기획 참고자료로 취급하고, 충돌 시 현재 사용자 지시를 따른다.

- UI: `D:\SVN\2_Documents\강현재\알파2 필요 업무\H.I 알파2 UI 개편 기획서 V3.7.xlsx`, `전투 씬 개편안` 워크시트와 첨부 도식.
- 스킬: `D:\SVN\2_Documents\강현재\H.I 캐릭터 스킬 기획서 V1.4.xlsx`, 저스티스·루미나·블랙 불릿·네코밍 워크시트.
- 강화 표: 저스티스 J34:J37, 루미나 J35:J38, 블랙 불릿 J34:J37, 네코밍 J34:J37. 16종 모두 기본/1강/2강/3강/4강/5강의 리스크 확률이 35/28/21/14/7/0%이다.
- `+` 상위 스킬은 습득에 따른 교체이고, 협회 강화 기본~+5의 6단계와 별개다. 기본형과 상위형은 같은 강화 효과를 공유한다. 강화 표를 일괄 피해량 증가표로 해석하지 않는다.

| 항목 | 구현 기준 |
|---|---|
| 히어로 버튼 | 항상 4칸. 획득 요구레벨 ≤ 캐릭터레벨이면 사용 가능 |
| 상위 스킬 | 습득 순서 1↔5, 2↔6, 3↔7, 4↔8을 같은 칸에 배치. 데이터의 교체 키로 연결 |
| 잠금 | 버튼 유지, 시전 비활성, 음각 음영·회색 반투명 오버레이·자물쇠. IP 부족과 구분 |
| 수동 시전 | 매회 스킬 선택 → 유효 대상 유닛 클릭 → 실행 후 선택 해제. 턴 시작 자동 선택 제거 |
| 필드 | 빈칸/필드를 시전 대상으로 추가하지 않음. 기존 점유칸→유닛 탐색도 실제 유닛이 있어야 함 |
| 협회스킬 | 화면 명칭만 변경하고 기존 무기스킬 데이터·비용·실행 경로 재사용 |
| 표시 | 현재 턴 노랑, 선택 가능 연두, 금지 검정, 효과 범위 빨강. 전체기는 대상 진영 빈칸까지 빨강 |
| 자동전투 | 기존 AI 선택 정책 유지. 수동으로 돌아올 때 이전 수동 선택 제거 |
| 공용 화면 | 메뉴·설정·확인·기존 스킬 획득 화면은 기존 호출 유지 |
| 연출 | 스킬명 말풍선까지. 캐릭터 애니메이션 수리와 새 시전 연출 제외 |

스킬 숫자와 설명은 실제 실행에 전달하는 데이터와 일치시킨다. 아직 구현되지 않은 리스크 효과를 UI만 표시하여 작동하는 것처럼 만들지 않는다.

## 실제 구조와 제약

1. 현재 HUD는 `Assets/_ProtoType_Merge/Scenes/TmpBattleScene.unity`에 직접 들어 있다. 이름만 보고 Resources의 BattleSceneCanvas 프리팹을 수정하면 대상 씬에 반영되지 않을 수 있다.
2. `InputHandler`와 KJ `SkillButtonController`는 기존 한 개 클래스 스킬을 자동 선택한다. 전투 캐릭터의 `ResolveSelectedSkill`은 영속 `CurrentSkillIndex`를 다시 읽으므로, 버튼 클릭 때 인덱스만 바꾸는 방식은 적합하지 않다.
3. 신규 전투 UI가 선택한 `SkillData`를 이번 행동의 값으로 전달하고, 비용·대상 검증·범위·실행이 동일한 값을 사용하도록 한다. 버튼 클릭으로 연구소 장착이나 세이브 데이터를 변경하지 않는다.
4. 직렬화 참조로 확인한 기존 카탈로그 테이블은 V3.1이다. V4.0 파일의 존재만으로 활성 데이터가 바뀌었다고 간주하지 않는다. V3.1에는 4명×8개=32개와 상위 16개의 교체 키가 있다.
5. `SkillExecutionRegistry`에 상위 16개(HS1011~HS4041)의 직접 등록이 없다. 상위 키가 기본 실행으로 빠지면 도발·회복·부활 등 전용 효과를 잃거나 실행에 실패할 수 있다. 교체 원본의 핸들러를 재사용하되 상위 스킬 자체의 ID·수치는 유지하는 연결이 필요하다. 기존 효과 전체의 기획 일치 여부를 검증한 것은 아니다.
6. HCS001~005 무기스킬 핸들러는 이미 등록되어 있다. 파일 상단의 '미연결' 주석보다 실제 코드를 기준으로 기존 연결을 재사용한다.
7. 기존 `OnActionExecuted`는 효과 적용 후 스킬명 문자열만 전달한다. 시전자 위치의 말풍선과 적 행동 범위 표시에는 시전자·스킬·대상/영역을 알 수 있는 실행 알림이 필요하다. 기존 완료 이벤트는 보존한다.
8. 공용 승패 프리팹은 DHScene_3·TutorialExploreScene·레거시 시뮬레이션 등에서도 참조한다. HPCanvas도 여러 플레이어 프리팹에서 참조한다. 원본을 꾸미지 않고 신규 전투 전용 프리팹을 연결한다.
9. 결과창은 `BattleResultPanel`이 생성·스킬 획득 순서·확인을 관리한다. 이 흐름을 유지하고, `HeroInfoResult`에 선택적인 전투 전용 표시 연결을 추가하는 방식이 우선이다. 새 표시 컴포넌트가 있는 경우에만 변경된 표시를 사용하고, 기존 소비자는 종전 동작을 유지한다.
10. 결과 미리보기에는 획득 EXP·전후 레벨·전후 IP가 있지만 EXP 바의 전후 현재량/요구량이 없다. 보상 확정 전에 읽기 전용 미리보기 값을 확보하고, 실제 지급 시점과 계산 규칙은 유지한다.

## 리소스 준비

- 원본: `D:\SVN\4_Resources\★ Alpha 02\2D\UI\BattleScene`.
- 프로젝트: `C:\Dev\HeroInfluence\Assets\_Ui_Sprites\BattleScene`.
- PNG 21개와 빈 안내 TXT 2개가 이미 프로젝트에 있고 원본과 해시가 일치한다. 추가 복사·새 복사 경로 설정은 필요하지 않다. 추후 필요하면 사용자가 직접 수행한다.
- HUD PNG 10개는 일반 Texture import 상태다. 실제 Image에 사용하는 파일에 한하여 Sprite 설정을 변경하는 최대 10개 `.png.meta`를 승인 후보로 잡는다. Tile 4개는 텍스처 사용이 가능하므로 일괄 변환하지 않는다. 결과창 7개는 이미 Sprite다.
- 기존 제공 스프라이트를 우선 사용한다. 잠금·단순 툴팁·빈 아이콘은 전투 전용 절차적 패널/텍스트로 보충한다. 장식 프레임은 적합한 경우 9-slice를 사용한다.
- 기존 아이콘 매핑에는 상위 키가 빠져 있다. 전투 전용 매핑에서 기본 아이콘+상위 표식으로 보충하고, 공용 SpriteLibrary는 수정하지 않는다.

## 제안 변경 범위

아래는 허용 목록 제안이며 아직 승인된 구현 범위가 아니다. 이름과 신규 파일 수는 책임 분리에 따른 예상이며 구현 중 확정한다. 기존 파일이 추가로 필요하면 편집 전에 이유와 범위를 확인받는다.

### A. 신규 전투 전용 파일

- 제안 코드 위치: `Assets/_ProtoType_Merge/JC/Scripts/UI/Battle/`.
- 제안 프리팹·레이아웃·표시 설정 위치: `Assets/_UI_Prefabs_Runtime/UI_BattleScene/`.
- 구성: HUD 조립/바인딩, 4슬롯 스킬 해석과 버튼, 선택 상태 표시, 툴팁, 턴 순서, 유닛 HP 표시, 타일 표시, 시작/턴 배너, 스킬명 말풍선, 결과 슬롯 표시.
- 예상: 신규 런타임 코드 약 10~14개, 프리팹 약 6~10개, 레이아웃/표시 설정 및 필요한 `.meta`. 파일 수를 맞추기 위한 분리는 하지 않는다.
- 의미 있는 EditMode 검증 추가: 4슬롯/레벨/상위 교체, 선택과 실행 데이터 일치, 행동 후 초기화, 보상 미리보기. 기존 테스트 구성에 맞추며 런타임 asmdef 재편은 하지 않는다.
- 신규 경로가 JC 아래여도 기존 공용 프레임워크에 대한 수정 권한을 의미하지 않는다.

### B. 사용자 확인이 필요한 기존 파일

경로 기준: `C:\Dev\HeroInfluence\Assets\_ProtoType_Merge\`.

| 수정 후보 | 필요한 최소 변경 | 기존 소비자 보호 |
|---|---|---|
| `ASB/Scripts/Battle/BinputHandler/InputHandler.cs` | 행동 단위 SkillData 선택, 자동 선택 해제, 선택/호버/초기화 알림, 동일 데이터로 비용·대상·실행 처리 | 신규 입력 방식은 대상 씬에서 활성화. 기존 API 경로와 자동전투 정책 유지 |
| `ASB/Scripts/Battle/Core/BattleManager.cs` | 실제 실행 승인 시 시전자·스킬·대상/영역 알림 | 피해 계산/턴 진행/기존 완료 이벤트 변경 없음 |
| `ASB/Scripts/Battle/Skill/SkillExecution/SkillExecutionRegistry.cs` | 상위 16개를 교체 원본 효과 핸들러에 연결 | 상위 수치와 ID 유지. 기존 기본형 및 HCS 등록 유지 |
| `KJ/Scripts/UI/HeroInfoResult.cs` | 선택적 전투 전용 결과 슬롯 표시 연결 | 새 표시 컴포넌트가 없는 기존 프리팹의 동작 유지 |
| `ASB/Scripts/Persistence/BattleRewardPlan.cs` | 결과 EXP 바용 미리보기 값 추가 | 저장 포맷 변경 없음 |
| `ASB/Scripts/Persistence/BattleResultPersistenceHandler.cs` | 기존 성장 규칙에 따른 EXP 전후 미리보기 생성 | 지급 공식·확정/저장 시점 변경 없음 |
| `Scenes/TmpBattleScene.unity` | 새 HUD/결과 프리팹/레이아웃 연결, 기존 씬 UI 중복 입력 제거, 카메라 기본 구도 조정 | 다른 씬과 공용 프리팹 원본 편집 없음 |
| `Assets/_Ui_Sprites/BattleScene`의 HUD `.png.meta` 최대 10개 | 실제 사용할 이미지의 Sprite import 설정 | PNG 내용/경로/복사 변경 없음 |

기본 산정은 기존 스크립트 6개, 씬 1개, import 설정 최대 10개다. 아래 2개는 조건부 후보로 별도 명시한다.

- `TargetingHelper.cs`, `HostageFriendlyFireResolver.cs`: 현재 공개된 SkillData 기반 조회를 재사용하는 것을 우선한다. 검증 규칙을 중복하지 않으려면 SkillData를 받는 유효성 검사 오버로드가 필요할 수 있다. 필요한 경우에만 기존 경로를 유지하는 추가로 제한한다.
- `BattleUIManager`, `BattleResultPanel`, `BattleResultView`는 기존 생성/확인 흐름과 새 프리팹의 필드 연결로 우선 재사용한다. 이 방식이 불가능하면 자동으로 편집 범위를 넓히지 않는다.

### C. 이번 전투 UI 범위와 별도 승인할 항목

- `LabManager`는 기본 1~최대 5의 5단계이며 유닛+개별 스킬 ID로 강화 상태를 보관한다. `LabModalController`도 이에 맞춰져 있다.
- 런타임 테이블의 ValueLv1~5는 피해/회복 수치를 올리는 구조다. V1.4의 6단계 리스크 감소 및 기본/상위 공유와 그대로 호환되지 않는다.
- SkillRiskIndex 필드는 있으나 조사한 실행 경로에서 해당 강화 표에 따른 리스크 확률 적용을 확인하지 못했다.
- 따라서 연구소의 6단계 강화, 기본/상위 강화 저장 공유, 기존 저장값 이관, 실제 리스크 효과 구현은 UI 결선보다 큰 공용 데이터/게임플레이 작업이다. 공용 SkillTooltip·세이브 스키마·카탈로그 교체까지 묶어 이번에 자동 수정하지 않는다.
- 제안: 이번에는 A/B의 전투 UI와 기존 실행 경로 연결을 먼저 진행하고, C는 별도 범위로 남긴다. 이 경우 V1.4 강화 시스템 전체를 구현 완료했다고 보고할 수 없다. 기존 강화값을 임의로 +단계에 환산하거나 서로 다른 저장값 중 최댓값을 자동 채택하지 않는다.
- 애니메이션 오류 수정, 신규 협회스킬 효과, 기존 히어로 효과 전면 수정, 밸런스 데이터 갱신 역시 별도다.

## 구현 순서와 완료 기준

1. **승인과 실행 환경 확인**: B와 조건부 후보의 사용자 확인 → 최신 Git/bridge 충돌 확인과 담당권 확보 → Unity 실제 상태 확인. 이 준비 작업에서 구현은 시작하지 않는다.
2. **스킬 선택 결선**: 레벨 1~8의 4슬롯 구성, 상위 교체, 잠금/IP 상태, 명시적 SkillData 전달부터 구현한다. 잘못된 스킬이 실행되지 않는 것이 첫 완료 기준이다.
3. **HUD 배치**: 기존 리소스 import와 전투 전용 Canvas/UILayout, 하단 정보·4스킬·협회스킬, 상단 메뉴/자동/배속/도주, 카메라 구도. 공용 버튼 동작은 재사용한다.
4. **전투 정보 표시**: 턴 순서 5칸, HP, 선택/호버/효과 영역, 툴팁. 전체기 빈칸의 빨간 표시와 빈칸 시전 금지를 함께 검증한다. 기존 GridCell 데이터 변경보다 전투 전용 표시 레이어를 우선한다.
5. **배너/말풍선**: 전투/턴 시작 배너와 실제 시전 스킬명을 표시한다. 적 행동도 같은 실행 알림을 사용한다.
6. **결과창**: 전투 전용 2×2 슬롯, EXP 바·레벨/승급 표시·IP 전후, 승패/확인. 기존 스킬 획득 화면 호출 순서와 보상 확정 흐름을 보존한다.
7. **검증**: 정적 컴파일과 의미 있는 EditMode 검증 후, 승인된 플레이 검증 범위에서 수동 확인한다. 연구소/공용 화면/다른 씬 회귀를 포함한다. 경로 추가가 필요하면 확인 후 확장한다.

주요 검증 사례:

- 4명 각각 레벨 1/4/5/8에서 슬롯 수·잠금·교체 키가 정확하다. 강화 단계와 상위 습득은 별개다.
- 턴 시작과 시전 완료 후 무선택. 스킬 변경 시 툴팁/비용/대상/실행 값이 함께 바뀐다.
- 잘못된 대상, 빈칸, UI 클릭, IP 부족, 이중 클릭, 대상 사망, 턴 변경, 자동↔수동 전환에 오시전/중복 행동이 없다.
- 공격뿐 아니라 회복·부활·인질 등 기존 대상 규칙이 유지된다. 전체기 빨간 빈칸은 시전 대상으로 취급되지 않는다.
- 상위 스킬이 원본 효과 핸들러를 사용하면서 상위 수치를 유지한다. 기존 HCS 경로가 유지된다.
- 결과창 확인 전 보상 미확정, 확인 후 한 번 확정. 레벨업/다중 레벨업/패배/전투 불능/최대 레벨의 EXP와 IP 표시가 맞는다.
- 다른 씬의 기존 결과창·HP바·공용 메뉴가 유지되고, 전투 재진입 시 이벤트/툴팁/패널이 중복되지 않는다.

## 준비 단계 검증 결과와 남은 승인

- 문서 2개와 4개 캐릭터 강화 표, 주요 실행/표시 코드, 활성 테이블과 프리팹 참조를 정적으로 확인했다.
- V3.1 데이터 32개 중 상위 16개에 교체 키가 있고 전용 실행 등록이 없는 것을 대조했다.
- 원본 SHA-256 전후 동일:
  - UI V3.7: `cd20a42c2d89f00a67a2ccf4429c7e8f97b53a7018620454443aa07edfb3ea83`
  - 스킬 V1.4: `473d6d1308f641272bf659c67dc6f033de35b6b1df8d76196a2a4efada6f4c13`
- 프로젝트 변경 없음. 리소스 복사 없음. 빌드·EditMode·Play 검증은 아직 수행하지 않았다. Unity 프로세스 존재만 확인되었으며 플레이 모드 상태는 확정하지 못했다.
- 추가 기획 질문은 없다. 구현 시작 전에 필요한 결정은 A/B 및 명시한 조건부 후보의 수정 승인과 C를 별도 작업으로 두는 범위 합의다. 이는 사용자의 공용/타팀 영역 사전 확인 지시에 따른다.
