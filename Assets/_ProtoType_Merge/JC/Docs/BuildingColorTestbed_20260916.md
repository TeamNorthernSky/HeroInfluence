# 건물 색상 테스트 씬 생성 — 2026-09-16

- 요청: 현재 씬의 라이팅 환경을 가진 BG 건물 종류별 테스트 씬 생성. 기존 환경 테스트 씬 재사용도 허용.
- 결과: `Assets/_ProtoType_Merge/JC/JC_TestScenes/z_JC_BuildingColorTestbed.unity` 신설. 기존 `z_JC_EnvTestbed`를 복사한 뒤 전시용으로 구성. 원본 테스트 씬 및 DHScene_3 미수정.
- 건물: 프리팹 19종 각 1개, 주택/연립·상점·오피스·고층·공장/창고 5그룹. 프리팹 연결·원본 크기·회전·재질 유지. 렌더러 바운드로 바닥 정렬 및 간격 확보. 광산 오브젝트 제외.
- 환경: DHScene_3의 저장된 RenderSettings 및 Light 직렬화 값 일치 확인(인스턴스 ID, 복사된 하늘 재질 참조 제외). 태양 강도 1.2, 회전 41.5/43.6, 환경광 0.55, 반사 강도 1, 반사 바운스 3. 원본 환경 프로파일과 하늘·색보정·DOF 프로파일은 `BuildingColorTestbed/` 아래 전용 복사본 사용.
- 비교 편의: 중성 회색 바닥, 건물별 받침과 이름표, 전체 조망 카메라. 기존 TestbedFreeCamera 재사용. 피사계 심도 객체는 보존하되 비활성(색상·디테일 관찰 목적). 백색광 전환은 아직 하지 않았음.
- 검증: 19종/19개 및 프리팹 연결 확인, 누락 스크립트 0, 재질·셰이더 연결 확인, 비플레이 카메라 렌더 확인. 씬 저장. 플레이 실행 없음.
- 카메라 배경은 중성 회색 단색으로 설정했으며 하늘의 환경광·반사 기여는 유지. 전시 카메라의 오클루전 컬링은 비활성. 직접 Camera.Render 반복 캡처에서 일부 표시 누락이 발생해, 카메라 행렬 초기화 및 URP SingleCameraRequest 방식으로 최종 렌더를 확인함. 최종 캡처 스크립트는 `verify-request.cs`.
- 작업 스크립트 및 화면: `C:/Dev/_scratchpad/bg-test-scene-713e6dbc41/`. 최초 이름표 폰트 로드 실패는 LegacyRuntime.ttf 사용으로 해결하고 생성 중인 새 씬에서 이어서 완료.
- 저장 과정에서 기존 메모리의 동적 TMP 폰트 2개가 디스크에 저장된 부수 변경을 확인함. 해당 저장본을 scratchpad에 보존한 뒤 시작 시 깨끗했던 추적 파일을 HEAD 내용으로 복원. 기존 사용자 소스 변경은 유지.
- 다음 단계: 전용 환경을 백색광 기준으로 조정하고 기준 UI 확정 후 대표 건물 색상 시안 제작. 텍스처 변경은 미착수.
